(define (problem test_problem)
    (:domain test_domain)

  (:objects )

  (:init 
   (user_initiative)
   )

  (:goal (and 
	  (FOIL_ACT_EXEC_dialogue-disambiguation-ask-for_clutch-seal-tightness)
	  (goal)
	  )
	 ))
