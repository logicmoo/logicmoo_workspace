(define (problem med-bw2)
    (:domain snlp-bw2)
  (:objects a b c d e)
  (:init (on c a) (on-table b) (on-table a)
	 (on e c) (on d b)
	 (clear d) (clear e))
  (:goal (AND (on a b)
	      (on b c)
	      (on c d)
	      (on d e)))
  (:length (:serial 5) (:parallel 5)))