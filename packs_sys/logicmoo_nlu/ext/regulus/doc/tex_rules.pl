% Rules for making plain ASCII into TeX

% Ordered list of defaults, strongest first.

orthography_rewrite("_", "\\_\\-").
orthography_rewrite("<", "$\\langle$").
orthography_rewrite(">", "$\\rangle$").
