
include('$REGULUS/Examples/Toy1/scripts/file_search_paths.cfg').

% The French generation grammar (can also be used for analysis)
regulus_config(regulus_grammar, [toy1_grammars(french)]).
regulus_config(top_level_cat, '.MAIN').

% Where to put the compiled generation grammar
regulus_config(generation_rules, toy1_runtime('generator.pl')).

regulus_config(working_file_prefix, toy1_runtime(french_generation)).

regulus_config(working_directory, toy1_runtime('')).
