service flashpolicy
{
		disable = no
		wait = no
		socket_type = stream
		flags = IPv6
		port = 843
		user = nobody
		group = nobody
		server = /usr/local/sbin/in.flashpolicyd.py
		server_args = -f/usr/local/etc/flashpolicy.xml
}
