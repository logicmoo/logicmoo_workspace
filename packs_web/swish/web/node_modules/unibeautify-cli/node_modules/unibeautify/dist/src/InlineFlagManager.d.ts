export declare class InlineFlagManager {
    private oldText;
    private newText;
    private readonly oldLines;
    private readonly containsDisable;
    constructor(oldText: string, newText: string);
    readonly text: string;
    private fixEndOfFile(text);
    private endsWithNewline(text);
    private readonly patch;
    private readonly rawPatch;
    private filterHunks(hunks);
    private shouldApplyHunk(hunk);
    private shouldIgnoreThisLine(lineNumber);
    private isDisabledAtLine(lineNumber);
    private codeAtLine(lineNumber);
}
