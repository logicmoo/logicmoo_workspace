import { OptionsRegistry } from "./options";
import { Language } from "./language";
import { Beautifier, BeautifierOptionName } from "./beautifier";
export declare class OptionsManager {
    options: OptionsRegistry;
    constructor(options: OptionsRegistry);
    /**
     * Get a shallow copy of the options currently loaded.
     */
    readonly loadedOptions: OptionsRegistry;
    /**
    Load Options
    */
    loadOptions(options: OptionsRegistry): OptionsManager;
}
export declare function optionKeys(beautifier: Beautifier, language: Language): BeautifierOptionName[];
