import { Dependency, NodeDependencyDefinition, DependencyOptions } from "./Dependency";
export declare class NodeDependency extends Dependency {
    protected definition: NodeDependencyDefinition;
    constructor(definition: NodeDependencyDefinition, options?: DependencyOptions);
    protected loadVersion(): Promise<any>;
    readonly package: any;
    private require(id?);
    private resolve(path);
    private resolveLocal(path);
    private resolveGlobal(path);
    private resolveWith(resolver);
    private fullPath(filePath?);
    private readonly packageName;
}
